GITHUB SETUP

GENERATE SSH PUBLIC KEY
check if an ssh key isn't already stored
/Users/jameselliott/git-repositories/git-practice/git-remotes.txt
$ cd ~/.ssh	// change current directory to .ssh (. indicates hidden file)
$ ls		// print working directory

Files with .pub extension will be the public key

If .ssh or .pub doesn't exist

$ ssh-keygen -o

The -o switch prompts for new password

